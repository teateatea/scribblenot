# Plan: Add `source` field and annotate `original` in add-task SKILL.md Step 3

## Task
#9 - Add `source` field to add-task categorization output

## Context
The add-task skill's Step 3 categorization output spec and the Agent prompt JSON schema do not currently include a `source` field to track whether a task was submitted by Joseph (the user) or Claude. Adding this field enables downstream tooling to distinguish user-initiated tasks from Claude-generated ones. The `original` field description also needs an annotation for clarity.

## Approach
Edit `~/.claude/skills/add-task/SKILL.md` directly:
1. In the Step 3 bullet list (the categorization output spec), annotate the `original` field description and add a new `source` field bullet.
2. In the Agent prompt JSON schema block, add a `source` key with allowed values `'Joseph'` or `'Claude'`.

No other files need to change. The Step 5 entry format already renders `Original:` as a label; that line is left untouched in this sub-task (it is a separate concern).

## Critical Files
- `C:/Users/solar/.claude/skills/add-task/SKILL.md`
  - Line 44: `- **original**: user's exact words (verbatim) -- passed through from input, not synthesized`
  - Lines 53-65: Agent prompt JSON schema block

## Reuse
No existing utilities to reuse; this is a documentation/spec edit only.

## Steps

1. **Annotate `original` field description and add `source` in the Step 3 bullet list (lines ~44-46).**

```diff
- - **original**: user's exact words (verbatim) -- passed through from input, not synthesized
- - **context**: the CONTEXT value from Step 1 -- passed through from input, not synthesized
+ - **original**: user's exact words (verbatim) -- label kept as "Original:" in the written entry; passed through from input, not synthesized
+ - **source**: who submitted the task -- `'Joseph'` if the task came from the user, `'Claude'` if generated by Claude
+ - **context**: the CONTEXT value from Step 1 -- passed through from input, not synthesized
```

2. **Add `source` to the Agent prompt JSON schema block (lines ~53-65).**

```diff
> - "section": best matching heading from: [SECTION_LIST]
> - "duplicate_of": matching task number or null
+ > - "source": "Joseph" or "Claude" -- who submitted the task
>
> Existing entries: [LINE1_LIST]
```

## Verification

### Manual tests
- Open `~/.claude/skills/add-task/SKILL.md` and confirm:
  - The Step 3 bullet list contains a `source` bullet with the description `'Joseph'` or `'Claude'`.
  - The Agent prompt JSON schema block contains a `"source"` key with the same description.
  - The `original` bullet is still present and its label text is unchanged.

### Automated tests
- Doc check: verify string presence after edit.

### Doc checks
- `C:/Users/solar/.claude/skills/add-task/SKILL.md | contains | **source**: who submitted the task`
- `C:/Users/solar/.claude/skills/add-task/SKILL.md | contains | "source": "Joseph" or "Claude"`
- `C:/Users/solar/.claude/skills/add-task/SKILL.md | contains | **original**: user's exact words`

## Changelog

### Review - 2026-03-24
- #1 (blocking): Fixed diff block 1 context mismatch -- removed incorrect leading space before `- **original**` and `- **context**` lines so the diff matches actual file indentation.
- #2 (minor): Replaced misleading "rename" language with "annotate" throughout (title, Context section, Approach section, Step 1 heading) -- the `original` field name is unchanged; only a clarifying note is added.
- #3 (nit): Fixed diff block 2 context line indentation -- reduced `   > ` to `  > ` to match actual file content.

### Prefect Pass - 2026-03-24
- P1 (minor): Fixed diff block 2 context line indentation -- removed remaining 2 leading spaces from `  > ` lines to match actual file content `> ` at add-task/SKILL.md:58-61.

## Progress
- Step 1: Annotated `original` field description and added `source` bullet in Step 3 bullet list of add-task/SKILL.md
- Step 2: Added `"source": "Joseph" or "Claude"` key to Agent prompt JSON schema block in add-task/SKILL.md
