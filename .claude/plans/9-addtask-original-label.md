# Plan: 9-addtask-original-label

## Task
#9 - Update `Original:` label to `{Joseph|Claude}:` in add-task SKILL.md Step 5

## Context
The `add-task` skill already captures a `source` field (Step 3) indicating whether a task was submitted by the user (`'Joseph'`) or generated by Claude (`'Claude'`). However, the Step 5 entry format still hard-codes the label `Original:` instead of using the source value. This plan makes the label dynamic so entries self-document who originated the task.

## Approach
Edit `~/.claude/skills/add-task/SKILL.md` in two places within Step 5: the 4-line top-level entry format block and the duplicate sub-entry format block. Replace the static `Original:` label with `{Joseph|Claude}:` (the actual value from the `source` field). Also add a brief inline note clarifying what each value means so future readers understand the convention without hunting through Step 3.

## Critical Files
- `C:/Users/solar/.claude/skills/add-task/SKILL.md` - Step 3 line 44 (stale inline comment on `original` field); Step 5, lines 82-83 and 91-92 (the two format blocks)

## Reuse
The `source` field is already produced by Step 3 categorization. No new logic is needed - this is purely a documentation/format change in the two entry templates.

## Steps

1. Open `C:/Users/solar/.claude/skills/add-task/SKILL.md` and locate Step 5's two format code blocks.

2. In **Step 3**, update the inline comment on the `original` field definition (line 44) to reflect the new label:

```diff
-- **original**: user's exact words (verbatim) -- label kept as "Original:" in the written entry; passed through from input, not synthesized
+- **original**: user's exact words (verbatim) -- label written as `{Joseph|Claude}:` in the entry (value from `source`); passed through from input, not synthesized
```

3. In the **4-line top-level entry format** block, replace the `Original:` label line:

```diff
- [ ] **#N** Brief description (Claude's synthesis)
  [D:XX C:XX] Claude's detailed interpretation of the task
-  Original: {user's exact words}
+  {Joseph|Claude}: {user's exact words}
  Context: {one-line description of what was being worked on when this was added}
```

4. In the **duplicate sub-entry format** block, replace the `Original:` label line:

```diff
  - [ ] **#M-2** Brief description
    [D:XX C:XX] interpretation
-    Original: {user's exact words}
+    {Joseph|Claude}: {user's exact words}
    Context: {context}
```

5. After the two format blocks (or in a short inline note within Step 5), add a clarifying sentence documenting the convention:

```
Use `Joseph:` when the task was submitted by the user; use `Claude:` when the task was generated autonomously by Claude.
```

## Verification

### Manual tests
- Run `/add-task` with a user-provided task description and confirm the written TASKS.md entry shows `Joseph:` as the label (not `Original:`).
- Run `/add-task` with a task sourced from Claude (e.g. via a subagent with `source: 'Claude'`) and confirm the entry shows `Claude:` as the label.
- Run `/add-task` with a duplicate task and confirm the sub-entry also uses the correct `{Joseph|Claude}:` label.

### Automated tests
- No automated tests exist for SKILL.md files. A script could grep TASKS.md after a test invocation and assert the absence of `Original:` and presence of `Joseph:` or `Claude:`.

### Doc checks
- `C:/Users/solar/.claude/skills/add-task/SKILL.md | missing | Original: {user's exact words}`
- `C:/Users/solar/.claude/skills/add-task/SKILL.md | contains | {Joseph|Claude}: {user's exact words}`
- `C:/Users/solar/.claude/skills/add-task/SKILL.md` | contains | `` `Joseph:` when the task was submitted by the user ``

## Changelog

### Review - 2026-03-24
- #1: Added Step 2 to update stale `original` field comment in Step 3 (line 44) that still referenced `Original:` label; renumbered subsequent steps; updated Critical Files to include line 44
- #2 (nit): Fixed malformed backtick span in Doc checks line 3 (`Joseph\`` was cutting off the check string)

### Review #2 - 2026-03-24
- #1 (minor): Doc check line 3 substring `Joseph: when` would not match the proposed backtick-wrapped form `` `Joseph:` when ``; updated check string to use double backticks so grep finds the literal backtick-wrapped label.

## Progress
- Step 2: Updated `original` field inline comment on line 44 to reference `{Joseph|Claude}:` label (value from `source`)
- Step 3: Replaced `Original:` with `{Joseph|Claude}:` in the 4-line top-level entry format block
- Step 4: Replaced `Original:` with `{Joseph|Claude}:` in the duplicate sub-entry format block
- Step 5: Added clarifying sentence after the format blocks: "Use `Joseph:` when the task was submitted by the user; use `Claude:` when the task was generated autonomously by Claude."
