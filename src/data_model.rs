// Data model helpers for the future data.rs split.
// Slice 0 scaffold only: authored config structs, runtime structs/enums, serde
// defaults, and lightweight inherent impls tied directly to those types will
// move here in later refactor slices. This file is intentionally empty during
// Slice 0 so that crate::data remains the public facade and no behavior changes.
