// YAML load and merge helpers for the future data.rs split.
// Slice 0 scaffold only: directory scanning, YAML document splitting,
// parse-to-merged-hierarchy flow, and top-level file merge rules will move
// here in later refactor slices. This file is intentionally empty during
// Slice 0 so that crate::data remains the public facade and no behavior
// changes.
