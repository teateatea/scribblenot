// Runtime conversion helpers for the future data.rs split.
// Slice 0 scaffold only: hierarchy-to-runtime transformation, flattening and
// navigation helpers, and child resolution plus body-mode inference will move
// here in later refactor slices. This file is intentionally empty during
// Slice 0 so that crate::data remains the public facade and no behavior
// changes.
